<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Task</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1>Edit Task</h1>
    
    <form action="/edit-task/<%= task._id %>?_method=PUT" method="POST" class="task-form">
      
      <div class="form-group">
        <label for="task-date">Task Date:</label>
        <input type="date" id="task-date" name="taskDate" value="<%= task.date.toISOString().split('T')[0] %>" required>
      </div>

      
      <div class="form-group">
        <label for="task-name">Task Description:</label>
        <input type="text" id="task-name" name="taskName" value="<%= task.name %>" required>
      </div>

      <button type="submit" class="btn submit-btn">Update Task</button>
    </form>

    
    <div class="back">
      <a href="/" class="btn back-btn">Back to Tasks</a>
    </div>
  </div>
</body>
</html>
